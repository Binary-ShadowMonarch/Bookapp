services:
  frontend:
    image: ghcr.io/${GHCR_OWNER}/bookapp-frontend:${IMAGE_TAG}

  backend:
    image: ghcr.io/${GHCR_OWNER}/bookapp-backend:${IMAGE_TAG}

  db-backup:
    volumes:
      - db_backups:/backups

volumes:
  db_backups:
